# 変更サマリ

- event_id: 20260617_014653_add_push_receive_mode
- 元USDM: 20260617_014653_add_push_receive_mode
- 生成日時: 2026-06-17T01:46:53
- 出典: GitHub Issue #11 変更要望: Producer push(put)型の受信モードに対応したい

## 追加

- 外部システム: 受信ディレクトリ（push(put)受信モードで Producer が直接ファイルを put する file-pubsub アクセス可能な共有/ローカルディレクトリ。fsnotify イベント駆動 + フォールバックポーリングで取り込む収集元）
- バリエーション: 取り込みトリガー方式（イベント駆動(fsnotify) / フォールバックポーリング。push 受信モードの取り込み契機）
- バリエーション: 完了検知方式（安定判定 / rename / doneマーカー。Producer の出力パターンに応じた書き込み完了の判定方式）
- BUC: ファイルを収集して配信するフロー → UC「ファイルを収集する(Collect)」にイベント「ファイルput受信（外部システム: 受信ディレクトリ）」を追加

## 変更

- 外部システム: Producerシステム → pull 型のリモートファイル領域への出力に加え、push(put)受信モードで受信ディレクトリへ直接 put する役割を追記
- 情報: 設定 → 収集ソース定義に収集モード(pull / push受信)・受信ディレクトリ・取り込みトリガー方式・フォールバックポーリング間隔・完了検知方式を指定できる旨を追記
- 情報: 収集ソース → 属性に ソース種別へ push受信(inbox) 追加・収集モード・受信ディレクトリパス・取り込みトリガー方式・フォールバックポーリング間隔・完了検知方式を追加し、バリエーションに 取り込みトリガー方式・完了検知方式 を追加。pull/push 両モードの説明に更新
- 情報: 処理済み管理 → push 受信モードの copy・done マーカー残置・イベント駆動とフォールバックポーリングの二重検知に対する重複取り込み防止に使う旨を追記
- 状態: 元ファイル収集状態 → 収集ソース領域に受信ディレクトリ(push)を含め、完了検知方式(安定判定/rename/doneマーカ)による「書き込み中→収集可能」、回収(push は受信ディレクトリ削除・done マーカー含む)・残す(copy)の遷移説明を更新
- 条件: 書き込み完了判定 → 完了検知方式(安定判定/rename/doneマーカー)を設定で選択する方式に拡張し、バリエーションに 完了検知方式 を追加
- 条件: 元ファイル処理判定 → push 受信モードでの回収(受信ディレクトリ削除)・残す(copy)、done マーカーの後始末を追記
- 条件: message_id採番 → イベント駆動とフォールバックポーリングの二重検知でも二重取り込みしない冪等性を追記
- バリエーション: 収集ソース種別 → 値に push受信(inbox) を追加し、pull型/push型の区分と排他選択を説明
- バリエーション: 元ファイル処理方式 → 回収の意味を pull型(GET後DELETE)/push型(受信ディレクトリ削除)に拡張、done マーカーの扱いを追記
- BUC: ファイルを収集して配信するフロー → UC「ファイルを収集する(Collect)」の説明を pull/push 両モード(ハイブリッドトリガー・完了検知方式)に更新

## 削除

- なし
