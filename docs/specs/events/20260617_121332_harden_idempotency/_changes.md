# 変更内容 (event 20260617_121332_harden_idempotency)

外部レビュー(Codex)指摘の冪等性・履歴保全ハードニング 2 件の差分仕様化(SPEC-007-01 / SPEC-014-02)。
UC「ファイルを収集する(Collect)」のみが影響。採番形式・正常系の挙動・後段(Archive / Fan-out / Manifest)は不変。

## 変更したファイル

| ファイル | 変更内容 |
|---------|---------|
| `ファイル配信業務/.../ファイルを収集する(Collect)/spec.md` | 計算ルール「message_id 採番」に同一秒衝突時の連番(_2, _3 …)付与を追記。分岐条件「元ファイル処理判定」に copy の処理済みマーカーは新契機にしない旨を追記。BDD に「同一収集秒に同名ファイルを再収集しても履歴を失わない」「marker・copy で残存マーカーは新本体の完了契機にならない」の 2 Scenario を追加 |
| `ファイル配信業務/.../ファイルを収集する(Collect)/tier-daemon-worker.md` | 処理フローの message_id 採番に連番付与(SPEC-007-01)、marker 完了判定に copy 残存マーカーの処理済み照合(SPEC-014-02)を追記 |
| `decisions/spec-decision-008.yaml` | (新規) ① 連番付与(サブ秒精度不採用)② marker+copy の処理済みマーカー除外、の判断記録 |

## 新規採番した ID

| ID | 種別 | 指すもの |
|----|------|---------|
| (SPEC-007-01 精緻化) | USDM 仕様 | 同一収集秒の同名再収集を連番付与で一意化し履歴を失わない |
| (SPEC-014-02 精緻化) | USDM 仕様 | copy で処理済みのマーカーは新たな完了契機にしない |

## トレーサビリティへの影響

RDRA 要素の追加・削除なし(条件「message_id採番」「元ファイル処理判定」の説明文を更新したのみ)。
要素数不変のため網羅率(100%)は維持。
