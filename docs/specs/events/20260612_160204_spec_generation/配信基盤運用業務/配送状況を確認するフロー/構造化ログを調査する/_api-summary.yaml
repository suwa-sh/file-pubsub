# _api-summary.yaml
# この UC に HTTP API はない(構造化ログは stdout / ファイルへの行指向 JSON。HTTP API は /metrics と /healthz の 2 つで別 UC の責務)
uc: "構造化ログを調査する"
business: "配信基盤運用業務"
buc: "配送状況を確認するフロー"
paths: []
async_events: []
