// Command file-pubsub is the single binary providing the daemon (serve) and
// the operations CLI (status / replay / config validate). Subcommands are
// implemented in Phase 2; this is a placeholder entry point.
package main

func main() {}
